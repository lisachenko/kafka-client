<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <!-- Pure unit tests, no external dependencies -->
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <!-- Tests against a real Kafka 0.11.0.3 broker, skipped unless KAFKA_BOOTSTRAP_SERVERS is set -->
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <!-- Replays the documented wire vectors of docs/protocol through the protocol classes, no broker needed -->
        <testsuite name="compliance">
            <directory>tests/Compliance</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
