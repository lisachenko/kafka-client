// JAAS configuration of the test broker: SASL/PLAIN users for the SASL_PLAINTEXT / SASL_SSL listeners.
// Test credentials only - do not use anywhere else.
KafkaServer {
    org.apache.kafka.common.security.plain.PlainLoginModule required
    username="admin"
    password="admin-secret"
    user_admin="admin-secret"
    user_kafkatest="kafkatest-secret";
};
